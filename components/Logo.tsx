import * as React from 'react';
import styled from 'styled-components';

const Logo: React.FC = (): JSX.Element => {
  return <img className='labelbox-logo' alt='logo' src='/static/images/logo.svg' />
};

export default Logo;